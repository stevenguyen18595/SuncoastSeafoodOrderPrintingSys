import { Time } from "@angular/common";
import { time } from "console";

export class SuncoasrOrder{
    orderNumber: string = "";
    listOfItemInOrder : string = "";
    orderTime: Date = new Date();
}